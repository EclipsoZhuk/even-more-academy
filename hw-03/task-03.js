class Person {
    constructor(name, age) {
        this.name = name;
        this.age = age;
    }

    getInfo() {
        return `${this.name}s age is ${this.age}`;
    }
}

// const p = new Person('Vlad', 27);
// console.log(p.getInfo());
